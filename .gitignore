# Whitelist mode
**/*

# Settings (hooks, etc)
!settings.json
!statusline-command.sh

# Core instructions
!/CLAUDE.md
!/skill-dependency-isolation.md

# Hooks registry
!/hooks/
!/hooks/*.sh
hooks/test-hooks.sh


# Plugins registry — track installed_plugins.json only (the reproducibility input).
# known_marketplaces.json is derivable from settings.json + installed_plugins.json and
# its lastUpdated timestamp churns on every /plugin background sync, so it's ignored.
!/plugins
!/plugins/installed_plugins.json


# Frictions (used by /learn)
!/friction/
!/friction/scripts/
!/friction/scripts/**
!/friction/domains/
!/friction/domains/**
!/friction/domain-map.json
# Exclude runtime data (ledger, logs, archives)
/friction/friction.jsonl
/friction/hook-activations.jsonl


# Memory store
!/memory/
!/memory/**


# Keep sub-directory gitignore files
!**/.gitignore


# Backlist files
**/.DS_Store
**/*.zip
